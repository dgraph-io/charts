{{- if .Values.validation.enabled }}
{{- /*
  The validator's expected state, derived from the chart's own values so it
  cannot drift from what was deployed. Rendered into a Secret rather than a
  ConfigMap: expected.json names the Secret keys that hold the admin/user
  passwords (adminPasswordKey, users[].passwordKey), so it is credential-adjacent
  and kept out of a world-readable ConfigMap. adminPasswordKey defaults to
  groot's key (the always-present superadmin); set validation.adminUser /
  adminPasswordSecretKey to validate as a different account. users carry their
  bootstrap passwordSecretKey.
*/}}
{{- $adminUser := .Values.validation.adminUser | default "groot" }}
{{- $adminKey := .Values.validation.adminPasswordSecretKey }}
{{- if not $adminKey }}
  {{- if eq $adminUser "groot" }}
    {{- $adminKey = .Values.alpha.acl.bootstrap.grootPasswordSecretKey }}
  {{- else }}
    {{- $adminKey = printf "%s_password" $adminUser }}
  {{- end }}
{{- end }}
{{- $users := list }}
{{- range .Values.alpha.acl.bootstrap.users }}
  {{- $users = append $users (dict "name" .name "passwordKey" .passwordSecretKey) }}
{{- end }}
{{- $groups := list }}
{{- range .Values.alpha.acl.bootstrap.groups }}
  {{- $groups = append $groups (dict "name" .name "rules" (.rules | default (list))) }}
{{- end }}
{{- $cronjobs := list }}
{{- if .Values.validation.checkBackups }}
  {{- if .Values.backups.full.enabled }}
    {{- $cronjobs = append $cronjobs (dict "name" (printf "%s-full" (include "dgraph.backups.fullname" .)) "schedule" .Values.backups.full.schedule) }}
  {{- end }}
  {{- if .Values.backups.incremental.enabled }}
    {{- $cronjobs = append $cronjobs (dict "name" (printf "%s-inc" (include "dgraph.backups.fullname" .)) "schedule" .Values.backups.incremental.schedule) }}
  {{- end }}
{{- end }}
{{- $expected := dict
  "namespace" (include "dgraph.namespace" .)
  "expectedAlphas" (.Values.alpha.replicaCount | int)
  "expectedZeros" (.Values.zero.replicaCount | int)
  "aclEnabled" .Values.alpha.acl.enabled
  "adminUser" $adminUser
  "adminPasswordKey" $adminKey
  "users" $users
  "groups" $groups
  "backups" (dict "check" .Values.validation.checkBackups "roundtrip" .Values.validation.backupRoundtrip "cronjobs" $cronjobs)
}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dgraph.alpha.fullname" . }}-validate
  namespace: {{ include "dgraph.namespace" . }}
  labels:
    {{- include "dgraph.labels" (dict "ctx" . "component" "validate") | nindent 4 }}
type: Opaque
stringData:
  expected.json: |
    {{ $expected | toJson }}
{{- end }}
